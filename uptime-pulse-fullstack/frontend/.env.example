# Base URL for the UptimePulse backend REST API.
# In development, Vite proxies "/api" to http://localhost:8080 (see vite.config.js),
# so this can usually be left as "/api". Override it for production builds
# that are served separately from the backend.
VITE_API_BASE_URL=/api
